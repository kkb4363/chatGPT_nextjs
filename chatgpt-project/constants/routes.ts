export const BASE_URL = "/";

export const AUTH_ROUTES = {
  LOGIN: "/login",
  SIGNUP: "/signup",
};

export const CHAT_ROUTES = {
  CONVERSATIONS: "/c",
};

export const PUBLIC_ROUTES = [AUTH_ROUTES.LOGIN, AUTH_ROUTES.SIGNUP];
